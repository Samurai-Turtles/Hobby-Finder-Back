package com.hobbyFinder.hubby.models.dto.situations;

import com.hobbyFinder.hubby.models.enums.RateSituationEnum;

public record UserRateSituationDto(
        RateSituationEnum rateSituationEnum) {
}
